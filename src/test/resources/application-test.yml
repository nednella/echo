spring:
    sql:
        init:
            schema-locations: classpath:sql/schema/schema-*.sql
            mode: always

cors:
    allowed-origins: '*' # comma-separated values

logging:
    level:
        org:
            springframework:
                web: DEBUG
                jpa:
                    core: TRACE
